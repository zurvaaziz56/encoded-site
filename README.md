# Encoded

Marketing site for Encoded — AI that follows your rules.
Live at [getencoded.co](https://getencoded.co).

Single-file static site (`index.html`), deployed on Vercel, auto-deploys on push to `main`.
